version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # SMTP Configuration (uncomment and configure to enable email sending)
      # - SMTP_HOST=${SMTP_HOST}
      # - SMTP_PORT=${SMTP_PORT:-587}
      # - SMTP_SECURE=${SMTP_SECURE:-false}
      # - SMTP_USER=${SMTP_USER}
      # - SMTP_PASS=${SMTP_PASS}
      # - EMAIL_FROM=${EMAIL_FROM:-3D Clinical <no-reply@3dclinical.com>}
      # - EMAIL_TO=${EMAIL_TO:-info@3dclinical.com}
    restart: unless-stopped
